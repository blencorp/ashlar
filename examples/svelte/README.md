# Ashlar Svelte Example

Svelte + Vite reference app proving Ashlar's generated CSS and source-owned Button capsule can be consumed without a Svelte-specific adapter.

```bash
pnpm --filter @ashlar/example-svelte dev
pnpm --filter @ashlar/example-svelte audit
```
