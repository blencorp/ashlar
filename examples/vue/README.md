# Ashlar Vue Example

Vue + Vite reference app proving Ashlar's generated CSS and source-owned Button capsule can be consumed without a Vue-specific adapter.

```bash
pnpm --filter @ashlar/example-vue dev
pnpm --filter @ashlar/example-vue audit
```
