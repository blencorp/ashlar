# Ashlar Vanilla Example

Vanilla TypeScript reference app for teams that do not want a framework. It imports the generated Ashlar stylesheet, uses semantic HTML, and keeps the Button capsule as source-owned markup.

```bash
pnpm --filter @ashlar/example-vanilla dev
pnpm --filter @ashlar/example-vanilla audit
```
