{
  "name": "@atrium/example-plain-html",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "node --check scripts/check.mjs",
    "test": "vitest run --passWithNoTests",
    "lint": "biome lint scripts"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
