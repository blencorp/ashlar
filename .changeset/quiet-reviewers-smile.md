---
"@blen/ashlar": patch
"@blen/ashlar-cli": patch
---

Make generated stable-evidence review bundles use the repository-local `pnpm ashlar` command path while documenting the published `ashlar` and `npx @blen/ashlar` alternatives for external reviewers.
