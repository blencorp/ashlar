---
"@ashlar/cli": minor
"ashlar": minor
---

Add shadcn-v4-style installer preview flags for `ashlar add`: `--view`, `--dry-run`, and `--diff` verify capsules before showing metadata, planned writes, or source diffs without changing consumer files.
