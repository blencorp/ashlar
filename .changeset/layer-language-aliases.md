---
"@blen/ashlar-cli": patch
---

Clarify registry layer language in CLI output and accept human-readable layer aliases such as `markup-primitives`, `interactive-components`, `service-patterns`, and `application-blocks` while retaining `L0`-`L4` as internal aliases.
