---
"@blen/ashlar-cli": patch
---

Add a GitHub Packages mirror readiness check that verifies BLEN-scoped package names, workflow registry/auth configuration, and the npmjs-first distribution split before publishing.
